using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartGallery : MonoBehaviour
{
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }

    private void OnMouseDown()
    {
        //button changes to grey
        

        //countdown from 3 appears, ending with "Go!"


        //clock countdown begins, starting from 5 seconds; targets can now be destroyed


        //Destroy(gameObject);


        //if target is successfully clicked on, then +1 to visable counter


        //when countdown reaches 0, message flashes on screen, button changes to green, targets reappear and countdown returns to 5 seconds
        //same effect happens if all targets destroyed before countdown reaches 0
        

        //if score is 3, show congrats text; if not, show sad-face text




    }
}
